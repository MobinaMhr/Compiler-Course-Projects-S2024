def f(a, b, [c = 10])
    if (true)
        return a+b;#It cannot infer return in if statement!
    end
    return a+b;
end
def main()
    b = [1, 3, 4];
    fptr = method(:f);
    b[fptr(1, 2)] = 10;
end
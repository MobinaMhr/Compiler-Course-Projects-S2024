def retList(n)
    a = [];
    for i in (0..n) # or for i in [n, n + 1, n + 2]
        next if (n / 2 == 0);
        push(a, i);
    end
    return a;
end

def main()
    a = retList(10);
    a[0] = "1";
    puts(a);
end